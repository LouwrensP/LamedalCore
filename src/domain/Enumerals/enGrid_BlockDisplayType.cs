namespace LamedalCore.domain.Enumerals
{
    public enum enGrid_BlockDisplayType
    {
        Address,
        Name,
        Value,
        Index,
        StateID,
        DB_Name,
        DB_ID,
        Proress
    }
}